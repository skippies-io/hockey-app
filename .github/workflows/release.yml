name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install (no lifecycle scripts)
        run: npm ci --ignore-scripts

      - name: Rebuild required native deps
        run: npm rebuild esbuild

      - name: Build
        run: npm run build

      - name: Prepare release assets
        run: |
          mkdir -p _release
          echo "${GITHUB_SHA}" > _release/commit.txt
          echo "$(date -u +"%Y-%m-%dT%H:%M:%SZ")" > _release/built_at_utc.txt
          echo "${GITHUB_REF_NAME}" > _release/tag.txt
          (cd dist && zip -r ../_release/dist.zip .)
          if [ -d specs ]; then
            (cd specs && zip -r ../_release/specs.zip .)
          fi

      - name: Upload release assets
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: false
          files: |
            _release/dist.zip
            _release/specs.zip
            _release/commit.txt
            _release/built_at_utc.txt
            _release/tag.txt
