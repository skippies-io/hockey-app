name: Guardrail (smoke)

"on": [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "hello"
