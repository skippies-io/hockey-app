# Supabase Production DB

Local testing note:

- If TLS verification fails when connecting to Supabase, set `PG_TLS_INSECURE=1` for local tests only.
Note: For local tests only, set `PG_TLS_INSECURE=1` if TLS verification fails.
