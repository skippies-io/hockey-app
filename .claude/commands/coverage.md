# coverage

Run coverage (used for Sonar new code coverage gates).

```bash
npm run test:coverage
```

Tip: if a PR fails Sonar on **new code coverage**, add tests for the new/changed files before re-running.